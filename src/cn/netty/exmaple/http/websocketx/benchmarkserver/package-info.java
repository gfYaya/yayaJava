/**
 * <p>This package contains a benchmark application for websockets.
 * <p>The websocket server will accept websocket upgrade requests and simply echo all incoming frames.
 * <p>The supplied index page contains a benchmark client that runs in a browser.
 * <p>Once started, you can start benchmarking by navigating to http://localhost:8080/ with your browser.
 * <p>You can also test it with a web socket client. Send web socket traffic to ws://localhost:8080/websocket.
 */
package cn.netty.exmaple.http.websocketx.benchmarkserver;